# Deployment manifest for Homelab Update Service on target Pi
device: local
target_dir: /opt/serve/homelab
bootstrap_script: update-homelab-updater.sh
only_copy_script: true

environment:
  HOMELAB_UPDATER_IMAGE: ghcr.io/sairam-suresh/homelab-update-service:latest
